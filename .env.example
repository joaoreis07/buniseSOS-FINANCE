# App
NEXT_PUBLIC_APP_NAME=BusinessOS Finance
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (Docker Compose — Etapa 2+)
DATABASE_URL=postgresql://businessos:businessos@localhost:5432/businessos_finance?schema=public

# Auth.js (Etapa 3+)
AUTH_SECRET=replace-with-a-long-random-secret
AUTH_URL=http://localhost:3000

# Email (Etapa 3+ — optional; console fallback in development)
RESEND_API_KEY=
EMAIL_FROM=BusinessOS <noreply@businessos.local>
